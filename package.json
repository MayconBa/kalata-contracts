{
  "name": "kalata-contracts",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "service": "hardhat run scripts/service.js",
    "compile": "hardhat compile",
    "hecoMainnet:deploy": "hardhat deploy --network hecoMainnet --no-compile",
    "mainnet:deploy": "hardhat deploy --network mainnet --no-compile",
    "mainnet:deploy": "hardhat deploy --network mainnet --no-compile",
    "testnet:deploy": "hardhat deploy --network testnet --no-compile",
    "ropsten:deploy": "hardhat deploy --network ropsten --no-compile",
    "rinkeby:deploy": "hardhat deploy --network rinkeby --no-compile",
    "goerli:deploy": "hardhat deploy --network goerli --no-compile",
    "kovan:deploy": "hardhat deploy --network kovan --no-compile",
    "localhost:deploy": "hardhat deploy --network localhost",
    "localhost:test": "hardhat --network localhost test",
    "localhost:node": "rm -Rf publish/deployed/localhost && hardhat node --no-deploy",
    "hardhat:deploy": "hardhat deploy --network hardhat",
    "hardhat:test": "hardhat test --network hardhat",
    "feeder": "hardhat run scripts/feeder.js --network localhost",
    "generateAccounts": "hardhat run scripts/generateAccounts.js",
    "partialTest": "hardhat test test/testFactory.js --network hardhat"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "@nomiclabs/hardhat-web3": "^2.0.0",
    "@openzeppelin/contracts": "^3.4.0",
    "@openzeppelin/contracts-upgradeable": "^3.4.0",
    "@openzeppelin/hardhat-upgrades": "^1.6.0",
    "@uniswap/v2-core": "^1.0.1",
    "@uniswap/v2-periphery": "^1.1.0-beta.0",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.3.0",
    "ethers": "^5.1.3",
    "fastify": "^3.15.0",
    "got": "^11.8.2",
    "hardhat": "^2.2.1",
    "child_process": "^1.0.2",
    "hardhat-deploy": "^0.7.5",
    "hardhat-gas-reporter": "^1.0.4",
    "hardhat-preprocessor": "^0.1.3",
    "node-schedule": "^2.0.0",
    "web3": "^1.3.5"
  },
  "dependencies": {}
}
