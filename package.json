{
  "name": "kalata-contracts",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "service": "hardhat run scripts/service.js",
    "compile": "hardhat compile",
    "testnet:service": "hardhat run scripts/service --network testnet",
    "testnet:deploy": "hardhat deploy --network testnet",
    "hecoMainnet:deploy": "hardhat deploy --network hecoMainnet",
    "mainnet:deploy": "hardhat deploy --network mainnet",
    "ropsten:deploy": "hardhat deploy --network ropsten",
    "rinkeby:deploy": "hardhat deploy --network rinkeby",
    "goerli:deploy": "hardhat deploy --network goerli",
    "kovan:deploy": "hardhat deploy --network kovan",
    "localhost:deploy": "hardhat deploy --network localhost",
    "localhost:test": "hardhat --network localhost test",
    "localhost:node": "rm -Rf publish/deployed/localhost && hardhat node --no-deploy",
    "localhost:service": "hardhat run scripts/service --network localhost",
    "localhost:partial-test": "hardhat test test/testFactory.js --network localhost",

    "hardhat:deploy": "hardhat deploy --network hardhat",
    "hardhat:test": "hardhat test --network hardhat",
    "checkContracts": "hardhat run scripts/checkContracts.js --network testnet",
    "generateAccounts": "hardhat run scripts/generateAccounts.js",
    "hardhat:partial-test": "hardhat test test/testFactory.js --network hardhat"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "@nomiclabs/hardhat-web3": "^2.0.0",
    "@openzeppelin/contracts": "^3.4.0",
    "@openzeppelin/contracts-upgradeable": "^3.4.0",
    "@openzeppelin/hardhat-upgrades": "^1.6.0",
    "@uniswap/v2-core": "^1.0.1",
    "@uniswap/v2-periphery": "^1.1.0-beta.0",
    "chai": "^4.3.4",
    "child_process": "^1.0.2",
    "ethereum-waffle": "^3.3.0",
    "ethers": "^5.1.3",
    "fastify": "^3.15.0",
    "got": "^11.8.2",
    "hardhat": "^2.2.1",
    "hardhat-deploy": "^0.7.5",
    "hardhat-gas-reporter": "^1.0.4",
    "hardhat-preprocessor": "^0.1.3",
    "node-schedule": "^2.0.0",
    "web3": "^1.3.5",
    "winston": "^3.3.3",
    "winston-daily-rotate-file": "^4.5.5"
  },
  "dependencies": {}
}
